resources:
  pipelines:
  - pipeline: apigee-proxy # Name of the pipeline resource.
    source: azure-pipelines # The name of the pipeline referenced by this pipeline resource.
    # project: xxxxx # Required only if the source pipeline is in another project
    trigger: true # Run apihub-pipeline when any run of apigee-pipeline completes


steps:
- bash: echo " apihub-pipeline runs after apigee-pipeline completes"